`backlinks` reports a duplicate when an accession-shaped id exists as two
headings, instead of walking it as a deed. Only a missing id falls through
to the accession scan; a clash and an I/O error stay errors.
