The HUD detail card has a Notes tab: the issue's logbook (timestamps,
state transitions, notes, CLOCK lines) and a composer that stays open
on that tab. `n` still opens the composer. `issue/get` and `show --json`
now include the same `logbook` array.
